# Configuración del viaje (lo único específico del viaje fuera de sus datos)
foto_base: https://ratgr.github.io/viajes-icons/     # base para imágenes relativas
fuente: ../viajes-icons/viaje.yaml                   # upstream de migrate_yaml (relativo a la raíz del repo)
